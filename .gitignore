# ── PRIVATE: model source and methodology ──────────────────────────────
# This repo is PUBLIC (it serves jtesports.com via GitHub Pages), so the CAR
# engine, its fitted weights and the rebuild reasoning stay out of it by
# default. Everything below lives in the working tree and is not published.
engine/
docs/
tools/
preview/
.claude/
flip-divergence-analysis.md
skills-lock.json

# ── large source data, regenerated by the refresh pipeline (data/README.md) ──
data/oe_2026_per_game_filtered.csv
data/lpl_per_game.csv
data/master_baseline.csv
data/master_rebuild.csv
data/master_baseline_teams.csv
data/master_rebuild_teams.csv
data/rebuild_diff.csv
deploy/

# ── build noise ──
__pycache__/
*.pyc
.playwright-mcp/
*.jpeg
